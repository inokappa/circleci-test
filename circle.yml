machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t inokappa/circleci-test .

test:
  override:
    - docker run -i -t -d inokappa/circleci-test /bin/bash ; sleep 3
    - docker exec `docker ps -q` /start.sh
