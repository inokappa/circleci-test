machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circleci/elasticsearch .

test:
  override:
    - docker run -i -d -p 9200:9200 circleci/elasticsearch; sleep 10
    - docker attach `docker ps -q`
    - pwd
