machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t inokappa/circlecitest .

test:
  override:
    - docker run -i -t -d inokappa/circlecitest /bin/bash ; sleep 10
    - echo "cd /root/serverspec && rake" | docker attach `docker ps -q`
